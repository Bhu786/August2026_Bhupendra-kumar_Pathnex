Ec2: 
| Instance Type         | Purpose                       | Example            |
| --------------------- | ----------------------------- | ------------------ |
| General Purpose       | Balanced CPU, RAM, Networking | t3, t4g, m5, m6i   |
| Compute Optimized     | High CPU workloads            | c5, c6i            |
| Memory Optimized      | Large RAM applications        | r5, r6i, x2idn     |
| Storage Optimized     | High-speed local storage      | i3, i4i, d3        |
| Accelerated Computing | GPU/AI/ML, Graphics           | p5, g5, inf2, trn1 |
| HPC Optimized         | Scientific computing          | hpc7g              |


==============================
aws S3 : 

=================
effect, action ,resources in policues in aws IAM 
IAM :roles polices  user group 

| Concept    | Purpose                                       | Example               |
| ---------- | --------------------------------------------- | --------------------- |
| **User**   | Individual person ya application              | Bhupendra, John       |
| **Group**  | Similar users ka collection                   | Developers, Admins    |
| **Role**   | Temporary permissions jo assume ki jaati hain | EC2 Role, Lambda Role |
| **Policy** | Permission document (JSON)                    | S3 ReadOnly Policy    |

=================================

nat agteway in publich subnet 

========================
Public IP:
Temporary
Restart ke baad change ho sakta hai.

Elastic IP:
Permanent Static Public IP
Restart ke baad bhi same public IP.

Network Interface (ENI)

Virtual NIC:
Contains
Private IP
Elastic IP
Security Group
MAC Address
==================
DNS Resolution:
AWS DNS

Automatically hostname resolve.
==================
VPC Endpoint:
Private access to AWS services.
Example

Private EC2

↓

S3

Without Internet.
Types:-
Gateway Endpoint (S3, DynamoDB)
Interface Endpoint (Most AWS services)

====================
VPC Peering:
2 VPC connect

Example:-
VPC A

↔

VPC B

=====================
Transit Gateway:

Bahut saare VPC connect:-
VPC1

VPC2

VPC3

↓

Transit Gateway

Hub and Spoke architecture.
=========================
| Security Group           | NACL                 |
| ------------------------ | -------------------- |
| Instance Level           | Subnet Level         |
| Stateful                 | Stateless            |
| Allow Only               | Allow + Deny         |
| Return traffic automatic | Return rule manually |
===========================
| Route                                     | Route Table                                       |
| ----------------------------------------- | ------------------------------------------------- |
| Single rule                               | Collection of routes                              |
| Tells where one type of traffic should go | Stores multiple routing rules                     |
| Example: `0.0.0.0/0 → IGW`                | Contains Local route + Internet route + NAT route |
| One entry                                 | Entire routing configuration                      |
============================
CIDR Block

Network ka IP range define karta hai.

Examples:
10.0.0.0/16
192.168.0.0/24
172.31.0.0/16
Interview: /16 ka matlab 65,536 IP addresses aur /24 ka matlab 256 IP addresses (251 usable in an AWS subnet).

==================
Subnet

VPC ka chhota network.

Types:

Public Subnet
Private Subnet

=====================
Internet Gateway (IGW)
VPC ko Internet se connect karta hai.
Sirf Public Subnet ke resources ko internet access deta hai (jab route table configured ho).

Without IGW

❌ No Internet
====================
NAT Gateway

Private subnet ko outbound internet access deta hai.
==============================