linux all interviwe questions
==================================
terrafrom : 
on single click we can create ec2 instance on aws without going aws portal 
==================================
1. created ec2 with terraform
some cmd:
terraform plan  
================================
cmd :
ps : runnong process
kill : kill <process name >
 kill with grep for finding id and with name of process

 kill -9 : forcefully kill (interview question)

 search process running|completed|error
 egrep -iv "running|completed|error" (not this matching )
 egrep  "running|completed|error" {matching given}
==============================================
 ping : see which server is active or not 
 ex: ping google.com
(in real company this cmd not working)
========================================
wget vs curl 
wget(dowwnload anythongs )
curl(to communicate to api)
=========================================
run level of linux : (may asked in interview)
0 → Off
1 → One user (Maintenance)
3 → Multi-user + Network (CLI)
5 → GUI/Desktop
6 → Restart
====================================================
#! : A shebang is the first line of a script, starting with #!. It specifies the interpreter that should execute the script, such as /bin/bash for shell scripts or /usr/bin/python3 for Python scripts. This allows the script to be run directly as an executable.
#!/bin/bash

echo "Hello World"

#!/usr/bin/python3

print("Hello World")

=========================================
