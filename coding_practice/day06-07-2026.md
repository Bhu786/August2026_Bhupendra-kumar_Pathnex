git clone : when we have nothing 
git pull : alreaady have need updated code from remote , latest changes 

git branch <name>: create branch
git checkout <name> : switch to branch
git checkout -b <name> : create with switch

git add . : add all current files
git add <file-name>
git commit -m "done"
git push 

git cherry-pick: bring specifi commit feom another branch 

commits related 2 imp. cmd:

reset:
Before

A → B → C → D

After

A → B → C

revert :
Before

A → B → C → D

After

A → B → C → D → Revert(D)

===================================




